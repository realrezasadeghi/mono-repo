{
  "name": "@reza/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "build": "npx lerna run build --stream"
  }
}
